import Game from "./game";
import Bridge from "./bridge";

class Avatar {
  constructor(i, j, ctx) {
    this.pos = [i, j];
    this.ctx = ctx;
  }

  makeMove(i ,j) {
    if (Bridge.bridge[i][j]) {

    }
    Game.
  }

  loadImage() {
    if (backgroundReady) {
      context
    }
  }
}

export default Avatar;