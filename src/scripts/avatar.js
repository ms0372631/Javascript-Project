import Game from "./game";
import Bridge from "./bridge";

class Avatar {
  constructor(i, j) {
    this.pos = [i, j];
  }

  makeMove(i ,j) {
    if (Bridge.bridge[i][j]) {

    }
    Game.
  }

  loadImage() {
    if (backgroundReady) {
      context
    }
  }
}

export default Avatar;